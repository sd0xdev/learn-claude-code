{
  "private": true,
  "name": "learn-claude-code",
  "description": "地下城與代理 — 透過實作文字冒險遊戲學習 Claude Code",
  "scripts": {
    "postinstall": "node scripts/init-dungeon.js",
    "start": "node reference/complete/server.js",
    "dev": "node scripts/init-dungeon.js && node dungeon/server.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.72.1",
    "dotenv": "^17.2.3"
  }
}
